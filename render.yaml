services:
  - type: web
    name: transport-analytics
    env: node
    buildCommand: |
      npm install
      python -m venv venv
      ./venv/bin/pip install -r requirements.txt
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
